<h2>(管理者)記事の詳細</h2>

<div>
	<p>タイトル：<%= @article.article_title %></p>
	<p>カテゴリー：<%= @article.article_category %></p>
	<p>
		<%= attachment_image_tag @article, :article_image, :fill, 200, 200, format: 'jpeg', fallback: "no_image.jpg", size: '200x200' %>
	</p>
	<p><%= @article.created_at.strftime('%Y/%m/%d') %></p>
	<p><%= @article.article_body %></p>
	<p><%= link_to "編集", edit_admin_article_path(@article.id) %></p>
	<p><%= link_to "削除", admin_article_path(@article.id), method: :delete %></p>
</div>