<h3>投稿の詳細</h3>

<div>
	<%= @post.user.profile_image %>
	<%= @post.user.nickname %>
	<br>
	<%= attachment_image_tag @post, :post_image, :fill, 200, 200, fallback: "no_image.jpg" %>
	<br>
	<%= @post.post_body %>
	<%= @post.created_at.strftime('%Y/%m/%d') %>

<div>
	<if user_signed_in? %>
		<% if @post.favorited_by?(current_user) %>
			<%= link_to user_post_favorites_path(@post), method: :deleted do %>
				<p>いいねから削除<span class="glyphicon glyphicon-heart-empty" aria-hidden="true" style="color: red;"></span><%= @post.favorites.count %>いいね</p>
			<% end %>
		<% else %>
			<%= link_to user_post_favorites_path(@post), method: :post do %>
				<p>お気に入りに追加<span class="glyphicon glyphicon-heart" aria-hidden="true"></span><%= @post.favorites.count %>いいね</p>
			<% end %>
		<% end %>
	<% else %>
			<p>いいね数<span class="glyphicon glyphicon-heart" aria-hidden="true" style="color: red;"></span><%= @post.favorites.count %></p>
</div>


	<p>応援する</p>

	<p><%= @post.comments.count %>件のコメント</p>
	<% if user_signed_in? %>
		<p><%= link_to '編集', edit_user_post_path(@post.id) %></p>
		<p><%= link_to '削除', user_post_path(@post.id), method: :delete %></p>
	<% end %>
</div>